services:
  postgres:
    image: postgres:15
    container_name: acctos-postgres
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: acctos_ai
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
